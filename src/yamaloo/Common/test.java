package yamaloo.Common;

import java.io.File;
import java.net.URL;

public class test {

	/**
	 * @param args
	 * @throws Throwable
	 */
	public static void main(String[] args) throws Throwable {

		// DBManager db = new DBManager();
		//
		// Site site = new Site("H&M Home", "H&M");
		// site = db.getSite(db.insertSite(site));
		// System.out.println(site.getSiteID());
		//
		// site = new Site("Muji Home", "Muji");
		// site = db.getSite(db.insertSite(site));
		// System.out.println(site.getSiteID());
		//
		// site = new Site("NikeStore", "Nike");
		// site = db.getSite(db.insertSite(site));
		// System.out.println(site.getSiteID());

	}

}